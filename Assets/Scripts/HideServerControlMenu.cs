using UnityEngine;

public class HideServerControlMenu : MonoBehaviour
{
    public void HideOnServer()
    {
        // if (isServer)
        // {
        gameObject.SetActive(false);   // hide on server only
                                       // }
    }
}
